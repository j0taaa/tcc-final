# Exact-commit tests

Reference-solver, oracle, differential, finite-slot, certificate, Rust-binding,
and decoder-integration tests live here. Random campaigns must record their
seeds and preserve minimized counterexamples as regression fixtures.
