[run]
seed = 42
backend = "python_reference"
strategy = "exact"

[scope]
kind = "top_k"
# This configuration is only a skeleton. Inject the tokenizer vocabulary size
# and included special-token IDs before executing a real run.
top_k = 8

[limits]
timeout_seconds = 30
