\documentclass[12pt]{article}

\usepackage{sbc-template}
\usepackage{graphicx,url}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[brazilian,english]{babel}
\usepackage{amsmath,amssymb,amsfonts,mathtools}
\usepackage{amsthm}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit}
\usepackage[hidelinks]{hyperref}

\sloppy
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}

\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\newtheorem{remark}{Remark}

\floatname{algorithm}{Algorithm}
\renewcommand{\algorithmicrequire}{\textbf{Input:}}
\renewcommand{\algorithmicensure}{\textbf{Output:}}
\renewcommand{\proofname}{Proof}

\newcommand{\ph}[1]{\textbf{[#1]}}
\newcommand{\mask}{\mathtt{[MASK]}}
\newcommand{\eos}{\mathtt{EOS}}
\newcommand{\pad}{\mathtt{PAD}}
\newcommand{\vocab}{\Gamma}
\newcommand{\lexalpha}{\Sigma_{\mathrm{lex}}}
\newcommand{\bytealpha}{\Sigma_{\mathrm{byte}}}
\newcommand{\Feas}{\mathcal{F}}
\newcommand{\Match}{\mathcal{M}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\neginf}{-\infty}
\newcommand{\argmax}{\operatorname*{arg\,max}}
\newcommand{\indic}{\mathbf{1}}

\title{Exact Maximum-Weight Parallel Commitment under Context-Free Grammars for Diffusion Language Models}

\author{Gabriel Jota Lizardo\inst{1}, \ph{Advisor name}\inst{1}}

\address{Computer Science Program\\
Pontifícia Universidade Católica de Minas Gerais (PUC Minas)\\
\ph{Campus and institutional address}
\email{\ph{email addresses}}}

\begin{document}

\maketitle

\begin{abstract}
Diffusion language models can reveal several positions in parallel, but independently plausible token proposals may be jointly incompatible with a context-free grammar. Existing CFG-constrained decoders can test whether a partial state remains completable, yet recent parallel methods select compatible proposal subsets heuristically. This work formulates maximum-weight parallel commitment as an exact weighted parsing problem. For a finite set of non-negative token proposals, it proves that the best compatible subset has the same value as the highest-reward grammar-valid completion. A max-plus parser therefore recovers both an optimal witness and the proposals that may be committed simultaneously. The formulation is first developed for token-aligned grammars and then generalized to a finite tokenizer-aware lattice composed with detokenization and lexical analysis. The resulting method targets soundness, completeness, per-step optimality, and finite-slot exactness. The practical evaluation will use \ph{model}, \ph{grammars/tasks}, and \ph{hardware}, comparing the exact method with exhaustive search on small instances and with heuristic constrained decoders on realistic instances.
\end{abstract}

\begin{otherlanguage}{brazilian}
\begin{resumo}
Modelos de linguagem por difusão podem revelar várias posições em paralelo, mas propostas de tokens individualmente plausíveis podem ser conjuntamente incompatíveis com uma gramática livre de contexto. Decodificadores recentes conseguem testar se um estado parcial ainda admite uma conclusão válida, porém a seleção de subconjuntos para compromisso paralelo permanece heurística. Este trabalho formula o compromisso paralelo de peso máximo como um problema exato de parsing ponderado. Para propostas de peso não negativo, demonstra-se que o melhor subconjunto compatível possui o mesmo valor que a conclusão gramaticalmente válida de maior recompensa. Um parser max-plus recupera, portanto, uma testemunha ótima e as propostas que podem ser comprometidas simultaneamente. A formulação é apresentada primeiro para gramáticas alinhadas aos tokens e depois generalizada para um lattice finito, sensível ao tokenizador, composto com detokenização e análise léxica. O método busca garantir correção, completude, otimalidade por etapa e exatidão quanto ao número real de slots. A avaliação prática utilizará \ph{modelo}, \ph{gramáticas/tarefas} e \ph{hardware}, comparando o método exato com busca exaustiva em instâncias pequenas e com decodificadores heurísticos em instâncias realistas.
\end{resumo}
\end{otherlanguage}

\section{Introduction}

Autoregressive language models generate a sequence from left to right, conditioning each new token on the previously generated prefix. Discrete diffusion language models use a different organization: they start from a corrupted sequence, often composed of masks, and perform successive denoising steps that estimate tokens for several still-unknown positions. Discrete diffusion processes were studied by \cite{austin2021d3pm}; simpler and more scalable masked-diffusion formulations were proposed by \cite{sahoo2024mdlm}; and larger models such as LLaDA showed that the approach can be applied to the large-language-model paradigm \cite{nie2025llada}. The ability to update several positions in one pass is a principal motivation for this paradigm.

Parallel generation also makes structural constraints harder to enforce. In code, JSON, expressions, domain-specific languages, and other formal outputs, validity is not an isolated property of each position. Two tokens may each be acceptable in isolation while becoming incompatible when fixed together. An autoregressive model can filter its next tokens from the current prefix. In a dLLM, masks remain in several regions, tokens may be revealed out of order, and every decision must preserve at least one valid completion of the entire canvas.

DINGO demonstrated constrained inference under regular languages through dynamic programming \cite{suresh2025dingo}. Later, \cite{mundler2025cfg} reduced CFG-constrained decoding to checking whether a partial output remains completable. Their method translates the partial state into a regular language of possible completions and tests whether its intersection with the CFG language is nonempty. EPIC reduced the cost of this approach through lexical memoization, Earley validation directly over a graph, and relaxed subset selection that recovers part of the parallelism \cite{jin2026epic}. However, when a full batch fails, identifying the best proposal subset remains a combinatorial choice handled by a heuristic policy.

This work asks: \emph{can we select exactly, without enumerating every subset, the maximum-weight set of parallel proposals that still admits a representable CFG-valid completion?} The central hypothesis is that enumeration over $2^{|C|}$ is unnecessary. Every valid completion automatically determines which proposals it preserves. Subset search can therefore be replaced by maximum-weight derivation search using weighted parsing and the max-plus semiring \cite{goodman1999semiring,mohri2002}.

The overall goal is to develop and demonstrate an exact parallel-commitment algorithm for CFG-constrained dLLMs. The specific goals are: (i) formalize \emph{Maximum-Weight Parallel Commitment} (MWPC); (ii) prove its equivalence to the highest-reward feasible completion; (iii) construct a token-aligned max-plus CKY version; (iv) generalize it to a finite lattice that preserves tokenization, lexical analysis, and provenance; (v) prove soundness, completeness, per-step optimality, and \emph{finite-slot exactness}; and (vi) implement and evaluate the method on \ph{selected tasks}.

The intended contribution is not weighted parsing itself, which is established in the literature, but the formulation of dLLM parallel commitment as a single weighted inference, the tokenizer-aware construction with finite slots, and its integration into an iterative decoder. The claimed optimality is local to the current step: the method maximizes the defined objective over the current proposals and possible completions of the current state. It does not claim to optimize the entire future trajectory because subsequent logits depend on the commitments made.

The rest of this article is organized as follows. Section~\ref{sec:fundamentacao} presents background and related work. Section~\ref{sec:problema} formalizes the problem. Section~\ref{sec:reducao} proves the main reduction. Sections~\ref{sec:tokenaligned} and~\ref{sec:lattice} describe the token-aligned and tokenizer-aware algorithms. Section~\ref{sec:decoder} integrates the optimizer into denoising. Section~\ref{sec:metodologia} specifies implementation and evaluation. Finally, Sections~\ref{sec:resultados}--\ref{sec:conclusao} cover the results to be filled in, limitations, and conclusion.

\section{Background and Related Work}
\label{sec:fundamentacao}

\subsection{Diffusion language models and parallel commitment}

Consider a finite token vocabulary $\vocab$ and a canvas of length $n$. A partial state is
\begin{equation}
 x \in (\vocab \cup \{\mask\})^n.
\end{equation}
At step $s$, the model estimates marginal distributions $p_\theta^{(s)}(\cdot\mid x)$ for masked positions. A reveal policy chooses proposed positions and tokens, usually from confidence, entropy, or the denoising schedule. Committing a proposal permanently replaces a mask with the corresponding token unless the sampler allows remasking.

Parallelism creates a distinction between \emph{proposing} and \emph{committing}. A model forward pass can propose many tokens simultaneously. Under a global constraint, however, the decoder must decide which proposals can become irrevocable without eliminating every valid completion. Serial validation tests one proposal, updates the state, and continues to the next; this is correct when every acceptance preserves a witness, but its outcome depends on order and may discard a better batch.

\subsection{Regular and context-free constraints}

Regular languages are representable by finite automata and capture local patterns, regular expressions, and many fixed schemas. DINGO formulates constrained inference for dLLMs under this class and uses dynamic programming to find or sample accepted sequences \cite{suresh2025dingo}. Later work provides exact inference over a mean-field distribution conditioned by finite automata, including sampling and greedy decoding \cite{dang2026automata}.

A context-free grammar $G=(N,\Sigma,P,S)$ has nonterminals $N$, terminals $\Sigma$, productions $P$, and start symbol $S$. CFGs represent recursive dependencies such as balanced parentheses, nested blocks, and syntax trees. CKY string recognition for a grammar in Chomsky normal form uses a chart over spans and is cubic in input length \cite{kasami1965}. Earley recognizes general CFGs without the same normalization and also has a cubic worst-case bound \cite{earley1970}. The intersection of a context-free language with a regular language remains context-free and can be constructed with states that combine nonterminals and pairs of automaton states \cite{barhillel1961,hopcroft2006}.

\subsection{Weighted parsing and semirings}

Parsing algorithms can be parameterized by a semiring. In the Boolean semiring, addition represents disjunction and multiplication represents conjunction, so the chart records reachability only. In the tropical max-plus semiring,
\begin{equation}
 (\R\cup\{\neginf\},\;\oplus=\max,\;\otimes=+,\;\mathbf{0}=\neginf,\;\mathbf{1}=0),
\end{equation}
the chart records the highest-scoring derivation. This abstraction unifies recognition, Viterbi, inside, and other quantities \cite{goodman1999semiring}. Weighted parsing algorithms and graph structures follow the same algebraic principles \cite{goodman1999semiring,mohri2002}.

This problem uses max-plus rather than sum-product. The distinction matters for the problem studied by \cite{amarilli2026probabilistic}: computing the total mass of CFG words under independent distributions may require unambiguity assumptions and become \#P-hard for general classes. Here, contributions from all words or derivations are not summed; only the best completion is sought. Provided that production weights do not artificially count ambiguity, multiple derivations of the same string do not change its maximum reward.

\subsection{Tokenization and the lexical interface}

Models operate over subwords produced by techniques such as BPE \cite{sennrich2016bpe} or SentencePiece \cite{kudo2018sentencepiece}. Programming-language grammars, by contrast, often use characters or categories such as \texttt{IDENTIFIER}, \texttt{NUMBER}, and \texttt{LPAREN}. One token may contain several characters, cross lexical boundaries, or complete a lexeme begun by another token. An exact method must simultaneously preserve model-token identity, emitted symbols, and the number of consumed slots.

If a program detokenizes first and discards provenance, two tokenizations that produce the same text become indistinguishable even when only one matches the proposal at a position. This work instead retains each choice as an arc in a token lattice and composes it with a detokenization and lexing relation. The lexical-analysis concepts of maximal munch, rule priority, and keywords versus identifiers follow compiler literature \cite{aho2006}.

\subsection{CFG-constrained decoding for dLLMs}

\cite{mundler2025cfg} define completion feasibility for a partial state: at least one way of filling the masks must produce an output in the target language. Their procedure builds a regular representation of possible completions of the partial output and decides whether its intersection with the CFG is nonempty. This formulation supports outputs with masks in several regions, but repeated validation introduces cost.

EPIC addresses three bottlenecks: repeated lexing, automaton determinization/minimization, and lost parallelism from serial checks \cite{jin2026epic}. For the last, it proposes a regular cover of the CFG and a heuristic split of the batch, followed by exact validation and serial fallback. The work notes that determining the truly compatible subset through subset checks would require exponentially many calls. This thesis changes the search variable: instead of choosing $B\subseteq C$ and repeatedly asking whether $B$ is valid, it puts all optional choices in one weighted structure and seeks a maximum-weight valid completion.

The positioning is therefore specific: DINGO and Dang--Ermon inference are exact for regular constraints; Mündler et al. provide an exact CFG completion-feasibility test; and EPIC recovers CFG parallelism through relaxed selection followed by verification. This work does not claim to invent weighted parsing or exact constrained inference in general. Its proposed contribution is to solve the maximum-weight CFG-compatible batch of the current step through one max-plus inference over a finite canvas, while recovering the witness and token provenance.

\section{Problem Formulation}
\label{sec:problema}

\subsection{Token sequences and feasible completions}

Let $\vocab$ be the model vocabulary and $n$ the number of canvas slots. The current state is $x\in(\vocab\cup\{\mask\})^n$. A token completion is $y\in\vocab^n$. We say that $y$ preserves $x$ when
\begin{equation}
 \forall i\in\{1,\ldots,n\},\qquad x_i\neq\mask \Rightarrow y_i=x_i.
\end{equation}

Let $\operatorname{detok}:\vocab^n\rightarrow\bytealpha^*$ denote detokenization. To support deterministic lexers or more general lexical relations, define
\begin{equation}
 \Lambda\subseteq\bytealpha^*\times\lexalpha^*,
\end{equation}
where $(b,\ell)\in\Lambda$ means that byte sequence $b$ can produce lexeme sequence $\ell$. The CFG $G=(N,\lexalpha,P,S)$ is defined over lexemes. Length, $\eos$, and $\pad$ conventions are represented by a regular predicate $H(y)$.

\begin{definition}[Feasible completion]
The set of feasible completions at state $x$ is
\begin{equation}
\Feas_x=\left\{y\in\vocab^n\;\middle|\;
\begin{array}{l}
y\text{ preserves }x,\ H(y),\text{ and there exists }\ell\in\lexalpha^*\\
\text{such that }(\operatorname{detok}(y),\ell)\in\Lambda
\text{ and }\ell\in L(G)
\end{array}\right\}.
\end{equation}
\end{definition}

This definition separates levels that must not be conflated: canvas position, model token, detokenized bytes, lexemes, and grammar terminals. The initial token-aligned version is the special case where $\vocab=\lexalpha$, $\operatorname{detok}$ and $\Lambda$ are identities, and $H$ only requires length $n$.

\subsection{Proposals, weights, and compatibility}

The proposal set is treated as an indexed family
\begin{equation}
 C=(c_1,\ldots,c_m),\qquad c_j=(i_j,t_j,w_j),
\end{equation}
with position $i_j$, token $t_j\in\vocab$, and weight $w_j\geq 0$. Indexing permits multiplicities, although the common policy produces one primary proposal per position. Weights may represent transformed confidence, priority, or unit value. If they are marginal probabilities, their sum must be interpreted as preservation utility rather than the joint probability of the sequence.

For a completion $y$, define
\begin{align}
\Match_C(y)&=\{j\in\{1,\ldots,m\}:y_{i_j}=t_j\},\\
R_C(y)&=\sum_{j=1}^{m}w_j\indic[y_{i_j}=t_j].
\end{align}

\begin{definition}[Compatibility]
An index set $J\subseteq\{1,\ldots,m\}$ is compatible if there exists $y\in\Feas_x$ such that $y_{i_j}=t_j$ for every $j\in J$.
\end{definition}

\begin{definition}[MWPC]
The maximum-weight parallel commitment problem is to compute
\begin{equation}
 J^*\in\argmax_{J\text{ compatible}}W(J),
 \qquad W(J)=\sum_{j\in J}w_j.
\label{eq:mwpc}
\end{equation}
\end{definition}

Explicit search examines up to $2^m$ sets. The problem, however, has additional structure: every compatible set has a completion as a witness, and every completion defines the proposals it matches.

\subsection{Assumptions and scope of the guarantee}

The main equivalence requires $w_j\geq 0$. With negative weights, a witness may match a proposal that should rationally be excluded; in that case, one can ignore nonpositive proposals or define $\Match_C^+(y)=\{j:y_{i_j}=t_j,\ w_j>0\}$. Strictly positive weights avoid committing zero-benefit proposals through a tie.

Optimization is conditioned on the represented support. If an implementation includes only the $K$ most probable tokens at each mask, the result is exact on that truncated lattice, not over all of $\vocab^n$. A full-vocabulary guarantee requires every relevant alternative to be represented explicitly or lazily without unsafe pruning.

Finally, MWPC chooses the best batch at the current step. Commitment changes the next forward-pass input and therefore the future distributions. No claim is made that $J^*$ maximizes cumulative reward through the end of denoising.

\section{Reduction to the Highest-Reward Feasible Completion}
\label{sec:reducao}

\begin{theorem}[Fundamental equivalence]
If $w_j\geq 0$ for every $j$, then
\begin{equation}
\max_{J\text{ compatible}}W(J)
=
\max_{y\in\Feas_x}R_C(y).
\label{eq:equiv}
\end{equation}
If $\Feas_x=\varnothing$, both problems are declared infeasible.
\end{theorem}

\begin{proof}
For any $y\in\Feas_x$, the set $\Match_C(y)$ is compatible because $y$ itself satisfies every proposal in that set. Hence,
\begin{equation}
\max_{J\text{ compatible}}W(J)
\geq
\max_{y\in\Feas_x}W(\Match_C(y))
=
\max_{y\in\Feas_x}R_C(y).
\end{equation}

Conversely, let $J$ be compatible. There is a witness $y\in\Feas_x$ that satisfies every proposal in $J$, so $J\subseteq\Match_C(y)$. Since all weights are non-negative,
\begin{equation}
 W(J)\leq W(\Match_C(y))=R_C(y)
 \leq\max_{y'\in\Feas_x}R_C(y').
\end{equation}
The inequality holds for every compatible $J$. Taking the maximum and combining both directions yields equality.
\end{proof}

\begin{corollary}[Optimal-batch recovery]
If $y^*\in\argmax_{y\in\Feas_x}R_C(y)$, then $J^*=\Match_C(y^*)$ solves MWPC.
\end{corollary}

\begin{proof}
$J^*$ is compatible because $y^*$ is its witness. Its weight is $R_C(y^*)$, which equals the MWPC optimum by Theorem~1.
\end{proof}

The theorem eliminates the need to represent subsets in the search space. The parser must find one valid completion whose reward is additive by position or arc. Backtracking that completion simultaneously provides a concrete compatibility certificate and the optimal set.

The grammar need not be unambiguous: because reward is attached to tokens or arcs and productions have neutral weight, distinct derivations of the same completion receive the same value. For ties, an optional extension maximizes lexicographically
\begin{equation}
\left(R_C(y),\ \sum_{i=1}^{n}\log p_\theta(y_i\mid x)\right),
\end{equation}
without changing optimality in the first coordinate.

\section{Token-Aligned Algorithm}
\label{sec:tokenaligned}

In this first version, each model token is a CFG terminal and the output has exactly $n$ terminals. Assume that $G$ is in Chomsky normal form, with productions $A\rightarrow BC$ and $A\rightarrow a$. Unit productions, empty productions, and special symbols must be removed or handled by a separate closure.

For each position $i$ and terminal $a$, define the lexical reward
\begin{equation}
s_i(a)=
\begin{cases}
\neginf, & x_i\neq\mask\text{ and }a\neq x_i,\\
\displaystyle\sum_{j:i_j=i,\ t_j=a}w_j, & \text{otherwise.}
\end{cases}
\label{eq:lexscore}
\end{equation}
The empty sum is zero. Thus, tokens incompatible with fixed positions are impossible, while permitted tokens receive exactly the sum of the proposals they satisfy.

Define $D[A,i,j]$ as the highest reward of a string derivable from $A$ that occupies positions $i,\ldots,j-1$. The base cases are
\begin{equation}
D[A,i,i+1]=\max_{A\rightarrow a\in P}s_i(a).
\label{eq:basecky}
\end{equation}
For longer spans,
\begin{equation}
D[A,i,j]=
\max_{A\rightarrow BC\in P}\ \max_{i<k<j}
\left(D[B,i,k]+D[C,k,j]\right).
\label{eq:cky}
\end{equation}
Impossible entries have value $\neginf$. Backpointers store the terminal, production, and split that attain each maximum.

\begin{algorithm}[ht]
\caption{Token-aligned MWPC through max-plus CKY}
\label{alg:cky}
\small
\footnotesize
\begin{algorithmic}[1]
\Require CFG $G=(N,\vocab,P,S)$ in CNF, state $x$, proposals $C$
\Ensure optimal batch $J^*$, witness $y^*$, and value $v^*$, or \textsc{Infeasible}
\State Initialize $D[A,i,j]\gets\neginf$ and empty backpointers
\For{$i\gets 1$ to $n$}
  \ForAll{$A\rightarrow a\in P$}
    \If{$s_i(a)>D[A,i,i+1]$}
      \State $D[A,i,i+1]\gets s_i(a)$; record $(\textsc{Terminal},a)$
    \EndIf
  \EndFor
\EndFor
\For{$\ell\gets 2$ to $n$}
  \For{$i\gets 1$ to $n-\ell+1$}
    \State $j\gets i+\ell$
    \ForAll{$A\rightarrow BC\in P$ and $k\in\{i+1,\ldots,j-1\}$}
      \State $v\gets D[B,i,k]+D[C,k,j]$
      \If{$v>D[A,i,j]$}
        \State $D[A,i,j]\gets v$; record $(B,C,k)$
      \EndIf
    \EndFor
  \EndFor
\EndFor
\If{$D[S,1,n+1]=\neginf$} \Return \textsc{Infeasible} \EndIf
\State $y^*\gets\textsc{Backtrack}(S,1,n+1)$
\State $J^*\gets\{j:y^*_{i_j}=t_j\}$
\State \Return $(J^*,y^*,D[S,1,n+1])$
\end{algorithmic}
\end{algorithm}

\begin{theorem}[Correctness of weighted CKY]
For every entry $D[A,i,j]$ computed by Algorithm~\ref{alg:cky}, its value is the maximum of $\sum_{r=i}^{j-1}s_r(y_r)$ over strings $y_i\cdots y_{j-1}$ derivable from $A$. Consequently, the algorithm returns a maximum-reward completion $y^*\in\Feas_x$ and an optimal MWPC batch.
\end{theorem}

\begin{proof}
The proof is by induction on length $j-i$. For length one, Equation~\ref{eq:basecky} examines every terminal production and assigns the correct score. For a longer span, every CNF derivation begins with some production $A\rightarrow BC$ and splits the string at $k$. By the induction hypothesis, both child entries contain the best values for their subspans; adding them yields the best derivation for that production and split. Taking the maximum over all choices covers every possible derivation. The start entry therefore represents the highest-reward feasible completion. Corollary~1 converts that completion into the optimal batch.
\end{proof}

If $|P_2|$ is the number of binary productions, a direct implementation takes $O(|P_2|n^3+|P_1|n)$ time and $O(|N|n^2)$ memory, in addition to backpointers. This version is the reference implementation: its output can be compared with enumeration of all subsets and all strings on small instances.

\section{Generalization to a Finite Tokenizer-Aware Lattice}
\label{sec:lattice}

\subsection{Token lattice and provenance}

A \emph{weighted token lattice} is a layered directed acyclic graph $A_{\mathrm{tok}}=(Q,E,q_0,F,\omega,\pi)$. For a canvas without early EOS, $Q$ contains layers $0,\ldots,n$; every arc from layer $i-1$ to $i$ corresponds to a token permitted in slot $i$. If $x_i$ is fixed, only its arc exists. If it is masked, there is one arc for every token in the represented support $S_i\subseteq\vocab$.

The weight of an arc labeled $a$ in slot $i$ is
\begin{equation}
\omega(i,a)=\sum_{j:i_j=i,\ t_j=a}w_j.
\end{equation}
The function $\pi(i,a)$ stores the indices of proposals satisfied by that arc. A complete path chooses exactly one token per slot, and the sum of its weights is $R_C(y)$. Provenance recovers $J^*$ without inferring it again from detokenized text.

When the model permits early termination, the lattice is composed with a regular constraint that accepts one occurrence of $\eos$ followed only by $\pad$, or it uses final states at different layers under explicit semantics. In either case, the number of consumed token arcs remains tied to the real canvas slots.

\subsection{Composition with detokenization and lexing}

Each token may emit a byte string. Conceptually, compose
\begin{equation}
A_{\mathrm{lex}}=
A_{\mathrm{tok}}\circ T_{\mathrm{detok}}\circ T_{\mathrm{lex}}\circ A_H,
\label{eq:composition}
\end{equation}
where $T_{\mathrm{detok}}$ maps tokens to bytes, $T_{\mathrm{lex}}$ maps bytes to lexemes, and $A_H$ implements EOS/PAD and other regular conditions. Weights and provenance originate on token arcs and are carried through composition. An implementation need not materialize the full composition; it may compute transitions lazily and share prefixes in a tokenizer trie.

For a programming-language lexer, the relation must reflect its declared policy, including maximal munch and priorities. Accepting every possible segmentation may recognize a lexical sequence that the real lexer would never produce. The initial scope will use \ph{chosen lexical strategy: byte-level grammar or deterministic lexer}, leaving complex contextual lexers as an extension.

\subsection{Max-plus parsing over the graph}

Let $Q$ be the state set of the resulting lexical graph. For a CFG in CNF, define
\begin{equation}
D[A,p,q]=\text{maximum weight of a path from $p$ to $q$ derivable from $A$}.
\end{equation}
For each terminal arc $e=(p,a,q)$,
\begin{equation}
D[A,p,q]\gets\max(D[A,p,q],\omega(e))
\quad\text{for every }A\rightarrow a.
\end{equation}
For binary productions,
\begin{equation}
D[A,p,q]=
\max_{A\rightarrow BC}\ \max_{r\in Q}
\left(D[B,p,r]+D[C,r,q]\right).
\label{eq:graphdp}
\end{equation}
The algorithm is the Viterbi version of CFG--automaton intersection. A weighted Earley implementation may avoid CNF and exploit top-down guidance, but it must preserve the same maximum and backpointer semantics.

\begin{algorithm}[ht]
\caption{Max-plus parsing over a finite lattice}
\label{alg:graph}
\small
\footnotesize
\begin{algorithmic}[1]
\Require CFG $G$, weighted lexical lattice $A=(Q,E,q_0,F,\omega,\pi)$
\Ensure best accepted path, derivation, provenance, and score
\State Initialize $D[A,p,q]\gets\neginf$ for $A\in N$ and $p,q\in Q$
\ForAll{arcs $e=(p,a,q)\in E$ and productions $A\rightarrow a$}
  \State relax $D[A,p,q]$ with $\omega(e)$ and record $e$
\EndFor
\Repeat
  \State $\textit{changed}\gets\textbf{false}$
  \ForAll{$A\rightarrow BC\in P$, $p,r,q\in Q$ compatible with DAG order}
    \State $v\gets D[B,p,r]+D[C,r,q]$
    \If{$v>D[A,p,q]$}
      \State update $D[A,p,q]$, record $(B,C,r)$, and set $\textit{changed}$
    \EndIf
  \EndFor
\Until{no entry improves}
\State $(f^*,v^*)\gets\argmax_{f\in F}D[S,q_0,f]$
\If{$v^*=\neginf$} \Return \textsc{Infeasible} \EndIf
\State reconstruct the derivation, token path $y^*$, and $J^*=\bigcup_{e\in y^*}\pi(e)$
\State \Return $(J^*,y^*,v^*)$
\end{algorithmic}
\end{algorithm}

The pseudocode makes the semantics explicit rather than prescribing the most efficient implementation. In a DAG, combinations can be ordered by path span; in Earley, items are processed by an agenda and reinserted only when their score improves. Closures for $\varepsilon$ and unit productions require a specific algebraic routine to avoid cycles without progress.

\begin{theorem}[Lattice soundness, completeness, and optimality]
Assume that: (i) the lattice represents exactly the token sequences permitted by the state and $H$; (ii) lexical composition represents exactly $\Lambda$; and (iii) the max-plus parser examines all derivations while preserving weights and provenance. Then Algorithm~\ref{alg:graph}: (a) returns only a completion in $\Feas_x$; (b) finds a completion whenever $\Feas_x$ is nonempty on the represented support; and (c) returns a maximum-weight MWPC batch on that support.
\end{theorem}

\begin{proof}
Every backtracking begins at $q_0$, ends at a final state, and follows existing arcs. By assumption (i), it corresponds to a permitted token sequence; by assumption (ii) and the derivation from $S$, its lexical output belongs to $L(G)$. This proves soundness. For completeness, every feasible completion corresponds to a lattice path and some CFG derivation, which the terminal and binary DP rules can reconstruct. For optimality, the induction used in Theorem~2 generalizes spans to state pairs: each entry contains the maximum over all compatible paths and derivations. The start value is therefore $\max_{y\in\Feas_x}R_C(y)$, and Theorem~1 yields MWPC.
\end{proof}

\begin{theorem}[Finite-slot exactness]
If every complete path through $A_{\mathrm{tok}}$ traverses exactly one token arc per permitted slot and composition neither creates nor removes token arcs, every returned witness is representable in the actual number of canvas positions.
\end{theorem}

\begin{proof}
By the layered construction, a start-to-finish path chooses one arc from layer $i-1$ to $i$ for each slot $i$. Internal detokenization or lexing transitions may emit zero or more symbols, but remain associated with the same token macro-arcs. Thus, no accepted path uses more slots than exist or depends on an abstract unbounded region.
\end{proof}

For an explicit graph with $q=|Q|$, a generic bound for the CNF version is $O(|P_2|q^3+|P_1||E|)$ time and $O(|N|q^2)$ memory. A naive lattice may have $O(n|\vocab|)$ arcs. Polynomial complexity therefore does not imply low cost; lazy composition, tries, and controlled support are important. When top-$K$ is used, the article must explicitly say ``exact on the truncated support $S_i$.''

\section{Iterative Decoder and Progress Guarantee}
\label{sec:decoder}

The optimizer solves one step, but a complete decoder must define proposals, budget, ties, fallback, and termination. Let $k_s$ be the maximum number of positions to commit at step $s$. The basic version chooses one primary proposal for each candidate position and applies MWPC. If $|J^*|>k_s$, a budgeted variant can be solved, or $C$ can first be restricted to positions chosen by the schedule.

The explicit variant adds a dimension $b$ to the chart:
\begin{equation}
D[A,p,q,b]=\text{best score using $b$ counted commitments}.
\end{equation}
For a binary rule, combine $b=b_1+b_2$. For $b\leq k$, memory grows to $O(|N|q^2k)$ and a direct implementation may take $O(|P_2|q^3k^2)$ time. With one primary proposal per position, $b$ counts preserved positions; multiple alternative proposals require an explicit budget definition.

An important case is $J^*=\varnothing$ even though a valid completion exists. The decoder cannot stall. Witness $y^*$ provides a safe progress fallback: committing its token at one masked position preserves that witness as feasible. This token may not be the model's primary proposal, but it guarantees progress without violating the grammar.

\begin{algorithm}[ht]
\caption{Exact parallel-commitment step}
\label{alg:step}
\small
\footnotesize
\begin{algorithmic}[1]
\Require model $M$, state $x$, CFG $G$, budget $k_s$, configuration $\Theta$
\Ensure new state $x'$ with completion feasibility preserved, or an infeasibility diagnostic
\State $P\gets M(x)$ \Comment{step logits/distributions}
\State $C\gets\textsc{GenerateProposals}(P,x,k_s,\Theta)$
\State $A\gets\textsc{BuildFiniteLattice}(x,C,\Theta)$
\State $(J^*,y^*,v^*)\gets\textsc{ParseMaxPlus}(G,A)$
\If{the instance is infeasible}
  \State expand support or execute the \ph{diagnostic/fallback policy}
\ElsIf{$J^*\neq\varnothing$}
  \State commit in $x$ the positions of proposals in $J^*$
\Else
  \State choose a masked position $i$ according to the \ph{fallback rule}
  \State commit $y^*_i$ at $x_i$
\EndIf
\State \Return $x$
\end{algorithmic}
\end{algorithm}

\begin{proposition}[Completion-feasibility invariant]
If $x$ has a feasible witness and a step commits only tokens matching a witness returned by the parser, then the new state still has a feasible witness.
\end{proposition}

\begin{proof}
The witness used at the step already preserves every fixed position in $x$. Since the new commitments match it, the witness also preserves the new state and continues to satisfy the lexical, grammatical, and slot conditions.
\end{proof}

\begin{proposition}[Termination]
Assume that the canvas has finitely many masks, remasking is disabled, and every feasible step commits at least one position. The decoder then terminates after at most the initial number of masks, and the final sequence belongs to the constrained language.
\end{proposition}

\begin{proof}
The number of masks strictly decreases and cannot be negative. At the final state, the witness that maintains the invariant must match every fixed token, so the output itself is valid.
\end{proof}

\section{Implementation and Evaluation Methodology}
\label{sec:metodologia}

Implementation follows three stages. \textbf{Stage A} builds token-aligned max-plus CKY with backtracking and a brute-force oracle for small instances. \textbf{Stage B} adds a layered lattice, provenance, EOS/PAD semantics, and composition with the \ph{chosen lexical interface}; weights may be synthetic or derived from saved logits. \textbf{Stage C} integrates the optimizer with the \ph{model used}, running candidate generation, parsing, and commitment at each step. The intended baseline is \ph{repository/baseline and commit}, reusing EPIC components where compatible.

The architecture separates the model adapter, candidate policy, lattice builder, detokenization/lexing interface, weighted parser, decoder, validators, and experiment scripts. The final article must describe the decisions connecting program and proof: grammar form, handling of $\varepsilon$ and unit productions, EOS/PAD semantics, per-position support, lexical policy, tie-breaking, fallback, and the weight definition.

\begin{table}[ht]
\centering
\caption{Implementation and reproduction fields.}
\label{tab:config}
\footnotesize
\begin{tabular}{p{3.2cm}p{8.0cm}}
\toprule
Item & Information to record \\ \midrule
Model/tokenizer & \ph{checkpoint and revision; vocabulary; special tokens; dtype/quantization} \\
Code/parser & \ph{repository, commit, license, languages, CKY/Earley, and backtracking} \\
Decoding & \ph{steps, schedule, top-$K$/support, budget, weight formula, and fallback} \\
Grammars/data & \ph{tasks, origin, sizes, transformations, prompts, and splits} \\
Environment & \ph{CPU, RAM, GPU/VRAM, OS, CUDA, PyTorch, compilers, and threads} \\
Protocol & \ph{seeds, repetitions, warm-up, synchronization, and reported statistics} \\
\bottomrule
\end{tabular}
\end{table}

The evaluation addresses five questions: (Q1) does the DP agree with brute force? (Q2) how much weight or cardinality do heuristics lose? (Q3) which abstract false positives are eliminated by finite slots? (Q4) how do runtime and memory scale? and (Q5) does the decoder work end to end? Baselines are brute force, exact serial validation, EPIC/heuristic selection, and unconstrained decoding where available.

For a heuristic $H$, the evaluation reports absolute gap $\Delta_H=W(J^*)-W(J_H)$ and, when $W(J^*)>0$, relative gap $\delta_H=\Delta_H/W(J^*)$. Other metrics include witness validity, oracle agreement, batch size, number of steps, fallback use, runtime, and peak RAM/VRAM. Profiling separates model forward, candidate construction, lattice construction, parsing, backtracking, and update. Comparisons run on the same hardware and also use normalized overhead $T_{\mathrm{method}}/T_{\mathrm{baseline}}$.

Automated tests check witness acceptance with an independent parser; matching of every committed proposal; equality between the reconstructed objective and chart score; immutability of fixed positions; compliance with slots/EOS/PAD; and complete agreement with brute force on small instances. The repository must contain locked dependencies, commands, configurations, raw results, and table-generation scripts.

\section{Preliminary and Expected Results}
\label{sec:resultados}

No result receives a value before its experiment is run. Table~\ref{tab:resultados} identifies the minimum set to fill in. Agreement below $100\%$ against brute force is treated as an implementation error or semantic disagreement, not statistical variation. The method need not outperform the heuristic in runtime: its principal contribution is to provide a certified optimum and characterize its cost.

\begin{table}[ht]
\centering
\caption{Results to complete after implementation.}
\label{tab:resultados}
\footnotesize
\begin{tabular}{p{4.2cm}p{3.0cm}p{4.0cm}}
\toprule
Group & Metric & Result \\ \midrule
Exactness & instances, agreement, and failures & \ph{values} \\
Witness & lexical, CFG, and slot validity & \ph{measured values} \\
Commitment & weight, cardinality, and gap & \ph{values by method} \\
Cost & runtime, overhead, RAM/VRAM & \ph{values and dispersion} \\
End-to-end & validity, steps, and fallback & \ph{values by task/model} \\
\bottomrule
\end{tabular}
\end{table}

The final analysis will discuss where the heuristic already reaches the optimum, when commitment gains justify overhead, which component dominates cost, at which sizes the lattice becomes limiting, and how many abstractly completable states are impossible within the real canvas.

\begin{table}[ht]
\centering
\caption{Proposed schedule, adjustable with the advisor.}
\label{tab:cronograma}
\footnotesize
\begin{tabular}{p{1.7cm}p{9.5cm}}
\toprule
Weeks & Activity \\ \midrule
1--4 & final formalization, CKY, backtracking, brute force, and tests \\
5--8 & lattice, provenance, EOS/PAD, and graph parser \\
9--11 & tokenizer-aware interface and integration with \ph{model} \\
12--14 & baselines, experiments, profiling, and analysis \\
15--16 & writing, reproduction, review, and defense preparation \\
\bottomrule
\end{tabular}
\end{table}

\section{Limitations and Threats to Validity}
\label{sec:limitacoes}

A CFG ensures syntax, not types, references, correct execution, or test satisfaction. Exactness also depends on representation: top-$K$ yields an optimum only on the truncated support; an approximate lexical interface limits the guarantee to the implemented semantics; and an explicit lattice may consume substantial memory. The objective sums local utilities and represents neither the sequence's joint probability nor an optimum over the full future trajectory. Configurations will state these limits. Brute force, an independent parser, property tests, recorded seeds, and reproducible artifacts mitigate errors; empirical conclusions remain conditioned on the \ph{models, grammars, data, and hardware used}.

\section{Conclusion and Future Work}
\label{sec:conclusao}

This article presented the theoretical basis of a decoder that replaces heuristic subset choice with one global weighted inference. The main result shows that, with non-negative weights, maximizing compatible-proposal weight is equivalent to finding the highest-reward feasible completion. In the token-aligned version, max-plus CKY solves the problem in polynomial time. In the tokenizer-aware version, a finite lattice preserves positions, tokens, weights, detokenization, lexing, and the actual number of slots; a max-plus graph parser recovers the witness and optimal batch.

The intended properties are verifiable independently of empirical gains: soundness guarantees that every returned batch has a witness; completeness guarantees that represented completions are not ignored; optimality guarantees maximum weight at the step; and finite-slot exactness excludes witnesses that require more tokens than the canvas provides. The iterative decoder preserves completion feasibility by induction and terminates when every step commits at least one mask.

The final version will fill in the \ph{main results} and discuss observed cost. Future work includes incremental parsing between steps, lazy composition with the full vocabulary, lexicographic objectives, an exact commitment budget, support for lightweight contextual constraints, and CFG-conditioned sampling. The latter must not be conflated with the maximization developed here because summing probability mass under ambiguous grammars has different difficulties.

\section*{Acknowledgments and AI-Use Statement}

\ph{Acknowledgments}. Artificial-intelligence tools were used for \ph{purpose of use}, contributing to \ph{affected sections}. Tool: \ph{name, provider, and version}. The author reviewed all content, proofs, references, and results and remains responsible for their correctness and originality.

\bibliographystyle{sbc}
\bibliography{referencias}

\end{document}
